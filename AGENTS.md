# win-cu-router 开发约定

优先读 README.md、PROVENANCE.md 和与任务相关的文档。默认简洁中文；按既有授权推进常规实现、验证和修复，不为每阶段重复索取确认。

- 每个实质改动在 `docs/changes/<任务>/` 留一份简短任务记录：意图、验收条件、方案、证据、审查与未完成项。小改动可合在同一文件，不强制生成六份文档。
- 新能力复用统一观察层；动作范围与完成 verifier 单独定义。不要把模型分数当权限，把“能读”当“能执行”，把接管当自主成功。
- 代码/配置改动运行 `npm run verify`；纯文档改动运行 `npm run verify:docs`。验证记录在 `.local/workflow/`，变更后旧指纹不再证明当前版本通过。
- 按 REVIEW.md 检查实际 diff 与证据，修复具体缺陷再跑相关检查。审查是宿主工作，不伪称自动调用了第二个模型或独立审查员。
- API、driver 和真实桌面验证与离线 CI 分开；只在任务授权范围执行，并记录真实核验结果、失败与计时边界。
- 截图与原始日志默认留 `.local`；公开证据须脱敏。保留上游贡献与启发，修改来源关系同步更新 PROVENANCE 与 THIRD_PARTY_NOTICES。
- Worktree 用于需要隔离的并行或独立验证工作；从已提交基线开始，不复制密钥、个人浏览器资料和供应商二进制。
- Hooks 是本地辅助检查，不能替代权限边界；不要自行修改 hook 信任记录、组织规则或全局安全设置。用户要求停止时停止，不让检查循环无限继续。

流程与当前启用状态见 docs/WORKFLOW.md。创建/发布远程仓库、合并和对外写入沿用用户的明确授权；当前工作流不自动发布或部署。
