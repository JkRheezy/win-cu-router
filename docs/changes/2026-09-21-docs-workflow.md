# 文档与工作流落地

## 意图与授权

用户要求：解释背景、优势劣势、事实依据与边界，准确记录上游贡献与启发；参考成熟开源项目的文档写法；在产出同时配置基于 SDLC Playbook 思路的 Codex 工作流。

本记录在实施过程中依据上述消息整理，不表示存在更早的独立审批、已合并的方案或外部审查。当前范围是本项目本地源码、文档、配置和检查，不包含创建远程仓库、对外发布、合并或额外付费模型调用。

## 验收

- 首页能说明适用对象、依赖、主要取舍和快速入口，来源具体到项目/提交。
- 样本成绩对应脱敏证据；失败和人工接管保留；不能把上游成果归为本项目原创。
- 本地验证命令、Git 提交检查、Codex hook 定义与环境命令可检查；源包不带私有文件。
- 明确区分手工测试、宿主启用和云端运行。没有证据时不填“已上线”。

## 方案与实现

重写 README 与 PROVENANCE，新增背景、写作参考、贡献说明和公开证据摘录。工作流采用一份任务记录、AGENTS/REVIEW、统一验证脚本、项目 Hooks、暂存区检查以及 GitHub CI/模板。沿用既有 TypeSafe / trycua 实测，无新的桌面执行基准。

## 验证与审查状态

公开执行摘要见 [workflow-validation](../evidence/workflow-validation-2026-09-21.json)。本机与隔离 Git worktree 都通过 16 项 JavaScript、7 项 Python 检查，以及文档链接/证据索引和发布扫描。六种 Hook 手工调用场景通过；暂存区检查拒绝了单独测试索引里的私有目录样本，且真实本地提交触发了 Git pre-commit。

隔离验证基线为 `fadc5550e823fd0a6de58990423adc775f8c7352`。新 worktree 从零建立开发环境后通过检查，没有复制 API key、driver、原始日志或个人浏览器资料。当前代码有本地提交和可检出的基线；未设置远程仓库或执行云端 CI。

初次 Codex 只读 app-server 查询返回本项目 0 项已加载 Hooks，日志提示项目尚未受信任，这是当时的记录。后续用户已完成信任，最新检查为两个事件 enabled/trusted；路径别名误判与修正见[启用核验记录](2026-09-21-hooks-activation.md)。环境 TOML 语法和命令通过，但桌面应用的环境选择状态仍未确认。

本次发现并修复：已有 uv 虚拟环境不带 pip，环境脚本现先执行必要的 ensurepip；Git 的自动换行可能影响跨平台钩子，加入 `.gitattributes`。没有修改用户全局配置或绕过 Hook 信任。

宿主自查关注：文档归属遗漏、把原生观察误称为自研能力、不同计时相除、过期验证、重复触发循环、私有目录进入提交或 CI 制品。独立审查和远程 CI 尚未进行。

## 后续边界

Hook 信任与本地环境选择属于 Codex 应用状态，需要在项目上下文分别确认；本次后续已确认前者。真实任意办公流程和长期运行仍是后续验收，不能由文档与离线工作流测试替代。
